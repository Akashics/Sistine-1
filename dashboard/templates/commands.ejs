<%- include('blocks/header', commands) %>

  <% const help = {};
 commands.forEach((command) => {
  const cat = command.category;
  if (!help.hasOwnProperty(cat)) help[cat] = [];
  help[cat].push(command);
});
%>
    <div class="container SistineBestContent">
      <% for (const category in help) { %>
      <br>
        <h2> <%= category %> Commands </h2>
        <div class="table-responsive" style="border-collapse: collapse; border-spacing: 0;">
        <br>
          <table class="table">
            <thead>
              <tr>
                <th>Command</th>
                <th>Usage</th>
                <th>Description</th>
                <th>Command Cost</th>
              </tr>
            </thead>
            <tbody>
              <% for (const command of help[category]) { %>
                <tr>
                  <td>
                    <%= command.name %>
                      <i>
                        <%= `${command.aliases.length > 1 ? `(${command.aliases.join(', ')})` : ''}` %>
                      </i>
                  </td>
                  <td>
                    <%= `${command.usageString.length > 1 ? command.usageString : 'No Arguments.'} `%>
                  </td>
                  <td>
                    <%= command.description %>
                  </td>
                  <td><%= command.cost > 1 ? command.cost + '¥' : 'Free' %></td>
                </tr>
                <% } %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
    <% include ./blocks/footer %>
